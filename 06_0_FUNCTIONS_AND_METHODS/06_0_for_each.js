//it is a method
//called higher order function also
let arr = ["pune", "delhi", "mumbai"];

// forEach loops through every element in the array
// 'val' represents each element on every iteration
arr.forEach((val) => {
    // toUpperCase() converts the string to all capital letters
    console.log(val.toUpperCase());
});